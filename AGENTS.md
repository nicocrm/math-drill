# Testing

- you SHOULD include unit tests and/or e2e tests when implementing new feature.  If not practical because the feature is too trivial, you MUST ask the user for confirmation to continue without unit tests.
- when proposing unit tests, you MUST NOT include unit tests for UI components.  These will be tested only using the e2e tests.


# Local dev uses HTTP-triggered worker (INGEST_WORKER_URL) instead of a message queue.
# Run: npm run dev:worker (in one terminal) and npm run dev (in another).
# No Docker services required for local development.
